# 中英文翻译标准（Translation Standards）

本书《How to Scale Your Model》中文翻译的统一标准。所有参与翻译的人员与翻译 agent **必须**遵循本标准，并与《翻译术语表》（`translation-glossary.md`）配合使用，确保全书术语一致、格式一致、技术含义准确。

- **适用对象**：人工译者、自动化翻译子 agent。
- **配套文件**：术语对照以 `translation-glossary.md` 为准，本书不重复罗列。

---

## 一、翻译总原则：信达雅

以「信达雅」为最高准则，三者冲突时的优先级为：**技术准确性 > 流畅性 > 文采**。

- **信（fidelity）**：准确传达原文技术含义。技术概念、逻辑、数值、单位不得歪曲；宁可直译清晰，不可为流畅而牺牲准确。
- **达（expressiveness）**：在准确的前提下，使用流畅、自然的中文表达，符合中文技术写作习惯，避免生硬的「翻译腔」与欧化长句。
- **雅（elegance）**：用词得体、简洁，章节与段落节奏清晰；但不为「雅」而牺牲「信」。

---

## 二、具体规则

### 2.1 原样保留（绝不翻译 / 转写）

以下元素**逐字保留英文 / 原文**，不翻译、不改写、不转写：

- **数学公式**：`$...$` 与 `$$...$$` 整段原样保留。
- **代码**：围栏代码块（```）、行内代码、代码标识符（`shard_map`、`psum`、`with_sharding_constraint` 等）原样保留。
- **URL 与链接**：完整保留，不翻译路径或锚文本中的技术片段。
- **保留英文的专有名词 / 缩写**：见术语表中标注「保留英文（KEEP ENGLISH）」的术语（如 `AllReduce`、`BF16`、`ICI`、`FSDP`、`JAX` 等）。

### 2.2 需要翻译的部分

- 正文叙述、图注（figure caption）、表格标题、`<d-footnote>` 内说明文字需翻译。
- 上述部分中的代码、公式、专有名词仍按 2.1 保留英文。

### 2.3 术语一致

- 同一英文术语全书只使用术语表给出的**唯一**中文译法，不切换同义词（例如不时而「分片」时而「切分」）。
- 凡列于术语表「保留英文」下的术语，全书统一写法与大小写，与原文一致。
- **大小写以原文为准**：源文大小写不统一时（如 `reduce-scatter` 与 `ReduceScatter` 混用），中文中按该处原文的写法**镜像保留**，不强行统一为一种大小写。

### 2.4 首现括注

- 翻译性术语**首次**出现时，可在中文后括注英文，如「全分片数据并行（FSDP）」「重计算（rematerialization）」。后续出现不再重复括注。
- 保留英文的术语（如 `TPU`、`AllReduce`）无须括注。

### 2.5 单位与数字

- `GB/s`、`TFLOPs`、`FLOPs/s` 等带单位写法原样保留；数字与单位间不留多余空格，保持原文风格。

### 2.6 标题与锚点

- 中文标题须带显式锚点：`## 标题文本 {#slug}`，其中 `slug = Jekyll slugify(标题文本)`。
  - 例：`## 时间花在了哪里？ {#时间花在了哪里}`
- 此举保证目录（TOC）的 `name:` 与正文交叉引用（`href="#..."`）能正确解析：kramdown 与 Jekyll `slugify` 对中文标点处理不同（如 `：`→`-`、`？` 被丢弃），必须用显式 `{#slug}` 统一。
- 文档内交叉引用链接须指向中文 `slug`，**不得残留英文 `slug`**（如 `#case-1-...` 应改为对应的中文 `slug`）。

---

## 三、双语呈现标准

本书采用**双语对照**呈现，对照方式定为：

- **并排 + 切换**：英文左栏、中文右栏并排显示；页面提供一键 `EN / 中` 切换，可整体在「仅英文 / 仅中文 / 并排」之间切换。（此为**目标呈现方式**，按本标准实现。）
- **文件组织**：每章由 `X.md`（英文原文）与 `X.zh.md`（中文译文）成对组成；构建时由拼接插件把 `X.zh.md` 的正文并入 `X.md` 页面。
- **纯中文页**：每个章节另提供纯中文独立页 `/<name>-zh/`（仅中文，无英文）。
- **baseurl**：本站 `baseurl: /scaling-book`，本地预览地址形如 `http://localhost:4000/scaling-book/<name>/`。

---

## 四、术语表引用

全文术语对照（硬件、并行策略、通信原语、训练 / 推理等）以《翻译术语表》`translation-glossary.md` 为准，**本书不重复罗列**。译者遇到术语时先查术语表；术语表未覆盖的新术语，按第二节规则拟定译法并补充进术语表。

---

## 五、使用说明与自检清单

### 5.1 使用流程

1. **翻译前**：通读本章英文，查阅 `translation-glossary.md` 确认术语译法。
2. **翻译时**：遵循第二节规则，数学 / 代码 / URL 原样保留，标题加 `{#slug}`。
3. **完成后**：按下方清单自检。

### 5.2 交付前自检清单

- [ ] **格式**：数学公式、代码块、行内代码、URL 均未改动。
- [ ] **术语**：全书术语与 `translation-glossary.md` 一致，无同义切换；KEEP-ENGLISH 术语大小写与原文一致。
- [ ] **锚点**：每个中文标题带 `{#slug}`；目录与正文交叉引用均指向中文 `slug`，无残留英文 `slug`。
- [ ] **图注 / 脚注**：图注与 `<d-footnote>` 已翻译，其中代码 / 公式 / 专名保留英文。
- [ ] **信达雅**：技术准确，中文流畅自然。
